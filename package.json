{
  "name": "hogaria",
  "version": "1.0.0",
  "description": "HogarIA: household, shopping, kitchen and AI on a single Raspberry Pi",
  "private": true,
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "dev": "concurrently --names CLIENT,SERVER --prefix-colors cyan,yellow \"pnpm --filter @hogaria/web run start\" \"pnpm --filter @hogaria/server run dev\"",
    "dev:client": "pnpm --filter @hogaria/web run start",
    "dev:server": "pnpm --filter @hogaria/server run dev",
    "build": "pnpm run build:server && pnpm run build:client",
    "build:client": "pnpm --filter @hogaria/web run build:prod",
    "build:server": "pnpm --filter @hogaria/server run build",
    "start": "pnpm --filter @hogaria/server run start",
    "test": "pnpm run test:client && pnpm run test:server",
    "test:client": "pnpm --filter @hogaria/web run test",
    "test:server": "pnpm --filter @hogaria/server run test",
    "test:e2e": "npx playwright test",
    "test:coverage": "pnpm run test:client:coverage && pnpm run test:server:coverage",
    "test:client:coverage": "pnpm --filter @hogaria/web run test:coverage",
    "test:server:coverage": "pnpm --filter @hogaria/server run test:coverage",
    "lint": "pnpm run lint:client && pnpm run lint:server",
    "lint:client": "pnpm --filter @hogaria/web run lint",
    "lint:server": "pnpm --filter @hogaria/server run lint",
    "lint:fix": "pnpm run lint:client:fix && pnpm run lint:server:fix",
    "lint:client:fix": "pnpm --filter @hogaria/web run lint:fix",
    "lint:server:fix": "pnpm --filter @hogaria/server run lint:fix",
    "format": "prettier --write \"**/*.{ts,json,md}\"",
    "check:ui": "node scripts/check-ui.mjs",
    "knip": "knip",
    "knip:fix": "knip --fix",
    "clean": "rm -rf node_modules frontend/node_modules server/node_modules frontend/dist server/dist",
    "audit": "pnpm audit --audit-level moderate",
    "audit:fix": "pnpm audit --fix",
    "docker:dev": "docker compose -f docker-compose.dev.yml up --build",
    "docker:dev:down": "docker compose -f docker-compose.dev.yml down",
    "docker:dev:clean": "docker compose -f docker-compose.dev.yml down -v --rmi local"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "concurrently": "10.0.5",
    "husky": "9.1.7",
    "knip": "6.35.1",
    "lint-staged": "17.5.1",
    "prettier": "3.9.6"
  },
  "lint-staged": {
    "*.{ts,json,md}": "prettier --write",
    "*.ts": "eslint --fix"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "onlyBuiltDependencies": [
    "better-sqlite3",
    "bcryptjs"
  ]
}
